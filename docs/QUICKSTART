Design Sync Toolkit - 零代码快速开始

安装步骤

方式一：作为 npm 包使用

bash

复制
# 安装到全局
npm install -g design-sync-toolkit

# 或在项目中安装
npm install design-sync-toolkit
方式二：本地开发

bash

复制
git clone <repository-url>
cd design-sync-toolkit
npm install
npm run build
基本用法示例

1. check - 单文件校验

验证单个设计意图 YAML 文件的格式合规性：
bash

复制
# 使用全局安装的 CLI
design-sync check intent.yaml

# 或使用 npx
npx design-sync-toolkit check intent.yaml

# 使用本地构建
node dist/presets/cli/index.js check intent.yaml
输出示例：
plain

复制
✅ 格式合规
   文件: intent.yaml
2. report - 批量扫描报告

扫描目录下的所有 YAML 文件并生成合规报告：
bash

复制
design-sync report ./intents/

# 输出到文件
design-sync report ./intents/ -o report.md
输出示例：
plain

复制
🔍 正在扫描目录: /path/to/intents/...

发现 3 个 YAML 文件

# 设计意图格式合规扫描报告
...
📊 扫描完成: 2 通过, 1 失败
3. debt - 遗留问题追踪

管理技术债务和遗留问题：
bash

复制
# 添加技术债务
design-sync debt add "登录页面响应时间过长" --owner "Alice"

# 列出所有未关闭的债务
design-sync debt list

# 列出所有债务（包括已关闭）
design-sync debt list --all

# 关闭债务
design-sync debt close DEBT-XXXX
YAML 格式示例

创建名为 intent.yaml 的文件：
yaml

复制
intent_id: "R001"
description: "用户登录流程优化"
layer: "input"
priority: "P0"
ownership:
  design: "Alice"
  product: "Bob"
status: "draft"
created_at: "2024-01-01"
updated_at: "2024-01-01"
version: "1.0.0"
dependencies: []
acceptance_criteria:
  - "3步完成登录"
  - "支持手机号和邮箱"
  - "错误提示友好"
tags: ["login", "ux", "auth"]
字段说明

表格


字段	必填	类型	说明
intent_id	是	字符串	唯一标识符，格式如 R001
description	是	字符串	设计意图描述
layer	是	枚举	input / validation / output
priority	是	枚举	P0 / P1 / P2
ownership	是	对象	包含 design 和 product 负责人
status	是	枚举	draft / reviewing / approved / rejected
created_at	是	日期	创建日期
updated_at	是	日期	更新日期
version	是	字符串	版本号，如 1.0.0
dependencies	否	数组	依赖的其他意图 ID
acceptance_criteria	否	数组	验收标准列表
tags	否	数组	标签列表
零代码路径说明

无需编写任何代码，仅使用文本编辑器和命令行即可：
用任意文本编辑器（VS Code、Sublime、记事本）创建 intent.yaml
按照上面的格式示例填写设计意图
运行自检命令：
bash

复制
# 校验格式
design-sync check intent.yaml

# 批量扫描当前目录
design-sync report .
就是这么简单！无需编程基础，任何人都可以使用本工具包标准化设计意图的表达。
